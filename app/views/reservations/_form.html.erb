<%= form_for([@tour, @tour.reservations.new]) do |f| %>
  <%= f.text_field :start_date %>
  <br/>
  <%= f.select :start_hour, '1'..'24' %>
  
  <br/>
  <%= f.submit "Book Now", class: "btn btn-normal"%>
<% end %>